[deploy]
healthcheckPath = "/setup/healthz"
healthcheckTimeout = 300
startCommand = "mkdir -p /data/.openclaw && cp /app/openclaw.json /data/.openclaw/openclaw.json && node openclaw.mjs gateway --allow-unconfigured --bind lan"

